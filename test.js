const { chromium } = require('playwright'); (async () => { const browser = await chromium.launch({ headless: false }); const page = await browser.newPage(); page.on('console', msg => console.log('BROWSER CONSOLE:', msg.type(), msg.text())); page.on('pageerror', err => console.log('BROWSER ERROR:', err)); await page.goto('http://localhost:8080'); await page.waitForTimeout(5000); await page.click('#authLoginBtn').catch(e=>console.log(e)); await page.waitForTimeout(5000); await browser.close(); process.exit(0); })();
